type unit_task = fn() -> i32;
type k0 = cont unit_task;

fn spawn() -> &k {
    k::new(worker);
} // wrap `worker` (a task)
fn prime(c: &k) -> &k0 {
    k0::bind(7, c);
} // bind the argument
fn go(c: &k0) -> i32 {
    c.resume();
} // run until it suspends
