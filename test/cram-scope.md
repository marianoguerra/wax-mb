# Cram tests: what this port runs

Generated by `waxdiff.py classify-cram`. A test is in scope when every
`wax` command it runs uses only flags and formats wax-mb implements;
anything else is listed below with the reason, so the excluded set stays
visible instead of shrinking unnoticed.

In scope: **2** of 328.

That is a small fraction, and the reasons below say why: the
reference's cram suite is overwhelmingly about the two things this
port does not implement -- conversion to and from wat/wasm, and the
type checker with its lints. The differential oracles are what cover
the front end, over 2112 corpus files; these tests add the CLI's own
behaviour (exit codes, which stream output goes to, flag handling).

## Excluded

| test | why |
|---|---|
| `all-errors-semantic-raise.t` | uses '--all-errors', which wax-mb does not implement |
| `arg-type-mismatch.t` | uses '--validate', which wax-mb does not implement |
| `array-len-bottom-ref.t` | converts to/from 'wat'; only wax is implemented |
| `array-new-data-datacount.t` | converts to/from 'wasm'; only wax is implemented |
| `array-new-fixed-large-count.t` | reads 'wat' input; only wax is implemented |
| `array-op-receiver-cast.t` | converts to/from 'wat'; only wax is implemented |
| `bare-export.t` | converts to/from 'wat'; only wax is implemented |
| `become-non-function.t` | `m.wax` parses but is rejected later, so the test needs the type checker |
| `binary-bad-type-index.t` | converts to/from 'wasm'; only wax is implemented |
| `binary-unbound-index.t` | reads 'wasm' input; only wax is implemented |
| `binop-flexible-number-operand.t` | converts to/from 'wat'; only wax is implemented |
| `block-binding-annotation.t` | reads 'wat' input; only wax is implemented |
| `block-dead-code.t` | reads 'wat' input; only wax is implemented |
| `block-exit-mismatch.t` | `br-and-fall-through.wax` parses but is rejected later, so the test needs the type checker |
| `block-kinds-inference.t` | reads 'wat' input; only wax is implemented |
| `block-missing-param-location.t` | reads 'wat' input; only wax is implemented |
| `block-nested-synthesis.t` | converts to/from 'wat'; only wax is implemented |
| `block-result-inference.t` | reads 'wat' input; only wax is implemented |
| `block-struct-trailing.t` | reads 'wat' input; only wax is implemented |
| `bottom-ref-validation.t` | uses '--validate', which wax-mb does not implement |
| `br-arity-no-crash.t` | `nonnull.wax` parses but is rejected later, so the test needs the type checker |
| `br-if-passthrough-cast-block.t` | converts to/from 'wat'; only wax is implemented |
| `br-if-passthrough-result.t` | converts to/from 'wasm'; only wax is implemented |
| `br-on-cast-chained-null-residual.t` | converts to/from 'wasm'; only wax is implemented |
| `br-on-cast-dead-code-source.t` | converts to/from 'wat'; only wax is implemented |
| `br-on-cast-fail-bottom-lub.t` | converts to/from 'wat'; only wax is implemented |
| `br-on-cast-fail-null-source.t` | converts to/from 'wasm'; only wax is implemented |
| `br-on-cast-multivalue.t` | converts to/from 'wasm'; only wax is implemented |
| `br-on-cast-unbound-label.t` | `bad.wax` parses but is rejected later, so the test needs the type checker |
| `br-on-non-null-empty-label.t` | reads 'wat' input; only wax is implemented |
| `br-on-null-bare-cast.t` | converts to/from 'wat'; only wax is implemented |
| `br-on-null-bare-null.t` | converts to/from 'wat'; only wax is implemented |
| `br-on-null-inferring-target.t` | converts to/from 'wat'; only wax is implemented |
| `br-table-bottom-ref-targets.t` | converts to/from 'wat'; only wax is implemented |
| `br-value-context-type.t` | `context-typed.wax` parses but is rejected later, so the test needs the type checker |
| `branch-hint-folded-roundtrip.t` | converts to/from 'wasm'; only wax is implemented |
| `branch-hint-invalid.t` | reads 'wat' input; only wax is implemented |
| `branch-hint-nested-cond.t` | converts to/from 'wasm'; only wax is implemented |
| `branch-hinting.t` | converts to/from 'wasm'; only wax is implemented |
| `bulk-segment-operands.t` | converts to/from 'wasm'; only wax is implemented |
| `cast-extern-convert.t` | exercises a lint, which needs the type checker |
| `cast-lattice-consistency.t` | converts to/from 'wasm'; only wax is implemented |
| `cast-v128-identity.t` | converts to/from 'wat'; only wax is implemented |
| `char-multibyte.t` | converts to/from 'wasm'; only wax is implemented |
| `check-all-errors.t` | uses '--all-errors', which wax-mb does not implement |
| `check-define.t` | uses '-D', which wax-mb does not implement |
| `check.t` | reads 'txt' input; only wax is implemented |
| `compact-import-groups.t` | converts to/from 'wasm'; only wax is implemented |
| `compact-import-item-id.t` | converts to/from 'wasm'; only wax is implemented |
| `compact-import-lowering.t` | converts to/from 'wasm'; only wax is implemented |
| `compact-import-section.t` | converts to/from 'wasm'; only wax is implemented |
| `compilation-hints-priority.t` | converts to/from 'wasm'; only wax is implemented |
| `compilation-hints-wat.t` | converts to/from 'wasm'; only wax is implemented |
| `compilation-hints-wax.t` | converts to/from 'wasm'; only wax is implemented |
| `compound-assign-hole-order.t` | converts to/from 'wat'; only wax is implemented |
| `compound-assignment.t` | converts to/from 'wat'; only wax is implemented |
| `cond-annot.t` | reads 'wat' input; only wax is implemented |
| `cond-define.t` | converts to/from 'wat'; only wax is implemented |
| `cond-export-name-split.t` | reads 'wat' input; only wax is implemented |
| `cond-if-only-references.t` | converts to/from 'wat'; only wax is implemented |
| `cond-validation.t` | uses '--validate', which wax-mb does not implement |
| `confusable-unicode.t` | converts to/from 'wat'; only wax is implemented |
| `const-expr-lint.t` | exercises a lint, which needs the type checker |
| `constant-condition-brif.t` | exercises a lint, which needs the type checker |
| `constant-trap-f32.t` | exercises a lint, which needs the type checker |
| `cont-new-const.t` | converts to/from 'wat'; only wax is implemented |
| `cont-new-exact.t` | reads 'wat' input; only wax is implemented |
| `convert-adaptive-operand.t` | converts to/from 'wat'; only wax is implemented |
| `convert-brnull-block.t` | converts to/from 'wat'; only wax is implemented |
| `custom-page-sizes.t` | converts to/from 'wat'; only wax is implemented |
| `data-numeric-values.t` | converts to/from 'wasm'; only wax is implemented |
| `dead-code-atomic-rmw-i32-widen.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-atomic-rmw-i64-addr.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-bottom-ref-call.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-bottom-ref-struct-cast.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-br-if-passthrough-i64.t` | converts to/from 'wasm'; only wax is implemented |
| `dead-code-chained-cast-residual.t` | converts to/from 'wasm'; only wax is implemented |
| `dead-code-flexible-result.t` | converts to/from 'wasm'; only wax is implemented |
| `dead-code-isnull-reconnect.t` | converts to/from 'wasm'; only wax is implemented |
| `dead-code-min-hole.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-ne-largeint.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-neg-select.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-passthrough-cast.t` | converts to/from 'wasm'; only wax is implemented |
| `dead-code-ref-ops.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-select-i31-get.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-select-width.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-simd-hole-pin.t` | converts to/from 'wat'; only wax is implemented |
| `dead-code-unknown.t` | `dead.wax` parses but is rejected later, so the test needs the type checker |
| `dead-code-width.t` | converts to/from 'wat'; only wax is implemented |
| `debug.t` | converts to/from 'wat'; only wax is implemented |
| `declare-ref-func.t` | converts to/from 'wasm'; only wax is implemented |
| `demote-float-method.t` | converts to/from 'wasm'; only wax is implemented |
| `descriptor-types.t` | uses '--validate', which wax-mb does not implement |
| `desugar-elem-declare.t` | converts to/from 'wat'; only wax is implemented |
| `desugar.t` | converts to/from 'wat'; only wax is implemented |
| `diagnostic-anchors.t` | exercises a lint, which needs the type checker |
| `dispatch.t` | converts to/from 'wat'; only wax is implemented |
| `docs-correspondence.t` | reads paths outside its own directory |
| `docs-examples.t` | reads paths outside its own directory |
| `docs-intro.t` | reads paths outside its own directory |
| `docs-language.t` | reads paths outside its own directory |
| `docs-wat.t` | reads paths outside its own directory |
| `drop-void.t` | converts to/from 'wasm'; only wax is implemented |
| `drop-width-drift.t` | converts to/from 'wat'; only wax is implemented |
| `duplicate-diagnostic-chains.t` | exercises a lint, which needs the type checker |
| `eager-select.t` | exercises a lint, which needs the type checker |
| `elem-declare-init.t` | converts to/from 'wasm'; only wax is implemented |
| `elem-kind-nonzero.t` | converts to/from 'wasm'; only wax is implemented |
| `empty-block-formatting.t` | `empty.wax` parses but is rejected later, so the test needs the type checker |
| `empty-stack-location.t` | uses '--validate', which wax-mb does not implement |
| `empty-struct.t` | converts to/from 'wasm'; only wax is implemented |
| `eqref-extern-convert.t` | converts to/from 'wat'; only wax is implemented |
| `error-format-json.t` | `bad.wax` parses but is rejected later, so the test needs the type checker |
| `error-format-short.t` | `bad.wax` parses but is rejected later, so the test needs the type checker |
| `exact-allocation.t` | uses '--validate', which wax-mb does not implement |
| `exact-func-import.t` | uses '--validate', which wax-mb does not implement |
| `exact-references.t` | uses '--validate', which wax-mb does not implement |
| `exnref-shorthand.t` | converts to/from 'wasm'; only wax is implemented |
| `extern-convert-any-nonnull.t` | converts to/from 'wat'; only wax is implemented |
| `extern-convert-const.t` | converts to/from 'wat'; only wax is implemented |
| `extern-convert-i31-get.t` | converts to/from 'wat'; only wax is implemented |
| `extern-convert-null.t` | converts to/from 'wat'; only wax is implemented |
| `f32-signaling-nan.t` | converts to/from 'wasm'; only wax is implemented |
| `f64-const-precision.t` | converts to/from 'wasm'; only wax is implemented |
| `failed-definition-recovery.t` | reads 'wat' input; only wax is implemented |
| `faithful.t` | converts to/from 'wat'; only wax is implemented |
| `feature-attribute.t` | converts to/from 'wasm'; only wax is implemented |
| `field-call-not-array-op.t` | converts to/from 'wat'; only wax is implemented |
| `float-literal-f32-range.t` | converts to/from 'wat'; only wax is implemented |
| `float-trunc-literal.t` | converts to/from 'wasm'; only wax is implemented |
| `fold-block-params.t` | uses '--fold', which wax-mb does not implement |
| `fold-errors.t` | uses '--fold', which wax-mb does not implement |
| `format.t` | converts to/from '-f'; only wax is implemented |
| `from-wasm-struct-new-non-struct.t` | converts to/from 'wat'; only wax is implemented |
| `func-inline-type-arity-mismatch.t` | reads 'wat' input; only wax is implemented |
| `func-inline-type-exact-ref-mismatch.t` | uses '-X', which wax-mb does not implement |
| `global-supertype-annotation.t` | converts to/from 'wat'; only wax is implemented |
| `guarded-export.t` | reads 'wat' input; only wax is implemented |
| `hinted-conditional.t` | uses '--validate', which wax-mb does not implement |
| `hinted-ref-func.t` | converts to/from 'wasm'; only wax is implemented |
| `hinted-wasm-to-wax.t` | converts to/from 'wat'; only wax is implemented |
| `hole-array-method.t` | converts to/from 'wat'; only wax is implemented |
| `hole-control-operand.t` | `match-scrut.wax` parses but is rejected later, so the test needs the type checker |
| `hole-order-cast-of-hole.t` | converts to/from 'wasm'; only wax is implemented |
| `hole-simd-lane-immediate.t` | converts to/from 'wat'; only wax is implemented |
| `hole-static-receiver.t` | converts to/from 'wat'; only wax is implemented |
| `hole-struct-reorder.t` | converts to/from 'wat'; only wax is implemented |
| `holes.t` | converts to/from 'wasm'; only wax is implemented |
| `huge-int-literal.t` | converts to/from 'wat'; only wax is implemented |
| `if-annotation-name-reservation.t` | converts to/from 'wat'; only wax is implemented |
| `if-binding-annotation-load-bearing.t` | reads 'wat' input; only wax is implemented |
| `if-binding-reinfer.t` | reads 'wat' input; only wax is implemented |
| `if-infer-construction.t` | reads 'wat' input; only wax is implemented |
| `if-infer-dead-fallthrough.t` | converts to/from 'wat'; only wax is implemented |
| `if-infer-largeint-join.t` | converts to/from 'wat'; only wax is implemented |
| `if-result-inference.t` | converts to/from 'wat'; only wax is implemented |
| `implicit-type-index.t` | reads 'wat' input; only wax is implemented |
| `implicit-type-numid-dedup.t` | converts to/from 'wat'; only wax is implemented |
| `implicit-type-ref.t` | converts to/from 'wasm'; only wax is implemented |
| `import-name-hint.t` | converts to/from 'wat'; only wax is implemented |
| `import-order.t` | uses '--validate', which wax-mb does not implement |
| `import-reorder-conditional.t` | converts to/from 'wat'; only wax is implemented |
| `inferred-block-float-literal.t` | converts to/from 'wat'; only wax is implemented |
| `inline-synthetic-type.t` | `string.wax` parses but is rejected later, so the test needs the type checker |
| `instr-conditional.t` | reads 'wat' input; only wax is implemented |
| `int-to-float-cast.t` | converts to/from 'wasm'; only wax is implemented |
| `isnull-on-bottom-ref.t` | converts to/from 'wat'; only wax is implemented |
| `label-name-folding.t` | converts to/from 'wasm'; only wax is implemented |
| `label-preserve.t` | converts to/from 'wat'; only wax is implemented |
| `labelled-arguments.t` | converts to/from 'wat'; only wax is implemented |
| `large-int-literal.t` | converts to/from 'wat'; only wax is implemented |
| `largeint-abstract-compare.t` | converts to/from 'wat'; only wax is implemented |
| `largeint-cast-to-i31.t` | converts to/from 'wat'; only wax is implemented |
| `largeint-float-ops.t` | converts to/from 'wasm'; only wax is implemented |
| `largeint-float.t` | converts to/from 'wasm'; only wax is implemented |
| `largeint-int-join-i64.t` | converts to/from 'wasm'; only wax is implemented |
| `largeint-narrowing-store.t` | converts to/from 'wat'; only wax is implemented |
| `largeint-unary-ops.t` | converts to/from 'wasm'; only wax is implemented |
| `largeint-wrap-to-i32.t` | converts to/from 'wat'; only wax is implemented |
| `leftover-width-drift.t` | converts to/from 'wasm'; only wax is implemented |
| `let-init.t` | uses '--validate', which wax-mb does not implement |
| `let-multi-recover.t` | converts to/from 'wat'; only wax is implemented |
| `let-multi.t` | uses '--validate', which wax-mb does not implement |
| `let-shadow-initializer.t` | converts to/from 'wat'; only wax is implemented |
| `lint-set-wat.t` | exercises a lint, which needs the type checker |
| `lint-set.t` | exercises a lint, which needs the type checker |
| `local-name-priority.t` | reads 'wat' input; only wax is implemented |
| `local-supertype-annotation.t` | converts to/from 'wat'; only wax is implemented |
| `loops.t` | converts to/from 'wat'; only wax is implemented |
| `lsp.t` | drives wax from a python3 script, so its invocations cannot be inspected |
| `match.t` | converts to/from 'wat'; only wax is implemented |
| `memarg-range.t` | converts to/from 'wasm'; only wax is implemented |
| `memory-copy-addrtype.t` | converts to/from 'wat'; only wax is implemented |
| `memory-name-shadowing.t` | converts to/from 'wat'; only wax is implemented |
| `module-name.t` | converts to/from 'wasm'; only wax is implemented |
| `multi-memory-memarg.t` | converts to/from 'wasm'; only wax is implemented |
| `nan-payload.t` | converts to/from 'wasm'; only wax is implemented |
| `narrow-store-i64-width.t` | converts to/from 'wat'; only wax is implemented |
| `neg-literal-overflow.t` | converts to/from 'wat'; only wax is implemented |
| `negative-const-lints.t` | exercises a lint, which needs the type checker |
| `nested-call-lowering-linear.t` | converts to/from 'wat'; only wax is implemented |
| `new-lints-wat.t` | exercises a lint, which needs the type checker |
| `new-lints-wax.t` | exercises a lint, which needs the type checker |
| `noextern-any-convert.t` | converts to/from 'wat'; only wax is implemented |
| `non-empty-stack-location.t` | `try-catch-leftover.wax` parses but is rejected later, so the test needs the type checker |
| `nonnull-bare-null.t` | converts to/from 'wat'; only wax is implemented |
| `null-hierarchy-nonany.t` | converts to/from 'wat'; only wax is implemented |
| `number-trunc-to-i32.t` | converts to/from 'wat'; only wax is implemented |
| `numeric-cast-cross-family.t` | `a.wax` parses but is rejected later, so the test needs the type checker |
| `omit-annotation.t` | `ambiguous-struct.wax` parses but is rejected later, so the test needs the type checker |
| `parse-error-messages.t` | reads 'wat' input; only wax is implemented |
| `precedence.t` | exercises a lint, which needs the type checker |
| `redundant-mul-zero-float.t` | exercises a lint, which needs the type checker |
| `redundant-neg-zero.t` | converts to/from 'wat'; only wax is implemented |
| `redundant-op-impure-operand.t` | exercises a lint, which needs the type checker |
| `redundant-sub-self-float.t` | exercises a lint, which needs the type checker |
| `ref-func-undeclared-same-type.t` | reads 'wasm' input; only wax is implemented |
| `ref-get-desc-exactness.t` | uses '-X', which wax-mb does not implement |
| `ref-ne.t` | converts to/from 'wat'; only wax is implemented |
| `ref-to-int-cast-always-fails.t` | converts to/from 'wat'; only wax is implemented |
| `reinterpret-literal.t` | converts to/from 'wasm'; only wax is implemented |
| `resume-handler-inference.t` | reads 'wat' input; only wax is implemented |
| `resume-operand-inferred-block.t` | converts to/from 'wasm'; only wax is implemented |
| `roundtrip-cast-desc-eq.t` | converts to/from 'wat'; only wax is implemented |
| `segment-name-handling.t` | converts to/from 'wat'; only wax is implemented |
| `select-branch-inference.t` | converts to/from 'wat'; only wax is implemented |
| `select-i64-width-pin.t` | converts to/from 'wat'; only wax is implemented |
| `select-largeint-branch.t` | converts to/from 'wat'; only wax is implemented |
| `select-type-mismatch.t` | `mismatch.wax` parses but is rejected later, so the test needs the type checker |
| `setter-not-a-value.t` | converts to/from 'wat'; only wax is implemented |
| `shift-count-nested-negation.t` | converts to/from 'wat'; only wax is implemented |
| `simd-lane-index-overflow.t` | `a.wax` parses but is rejected later, so the test needs the type checker |
| `simd-name-clash.t` | converts to/from 'wasm'; only wax is implemented |
| `simd-roundtrip.t` | converts to/from 'wat'; only wax is implemented |
| `simd-trunc-sat-zero.t` | converts to/from 'wat'; only wax is implemented |
| `sink-let-into-init.t` | reads 'wat' input; only wax is implemented |
| `source-map-mappings.t` | converts to/from 'wasm'; only wax is implemented |
| `source-map.t` | converts to/from 'wasm'; only wax is implemented |
| `stack-switching-errors.t` | uses '--validate', which wax-mb does not implement |
| `stack-switching.t` | reads 'wasm' input; only wax is implemented |
| `string-char-literals.t` | converts to/from 'wasm'; only wax is implemented |
| `string-global-export-index.t` | converts to/from 'wasm'; only wax is implemented |
| `string-i16.t` | converts to/from 'wasm'; only wax is implemented |
| `struct-default-abstract-reftype.t` | converts to/from 'wat'; only wax is implemented |
| `struct-punning.t` | converts to/from 'wat'; only wax is implemented |
| `struct-splice-decompile.t` | converts to/from 'wasm'; only wax is implemented |
| `struct-splice.t` | converts to/from 'wat'; only wax is implemented |
| `structured-try.t` | converts to/from 'wat'; only wax is implemented |
| `suggestions.t` | exercises a lint, which needs the type checker |
| `suspend-bind-operand-inferred-block.t` | converts to/from 'wasm'; only wax is implemented |
| `switch-chain-dup.t` | `m.wax` parses but is rejected later, so the test needs the type checker |
| `switch-tag-continuation.t` | reads 'wat' input; only wax is implemented |
| `syntactic-checks.t` | uses '--validate', which wax-mb does not implement |
| `syntax-error-fix.t` | reads 'wat' input; only wax is implemented |
| `tag-no-payload.t` | converts to/from 'wasm'; only wax is implemented |
| `tail-call-intrinsic.t` | converts to/from 'wat'; only wax is implemented |
| `text-type-errors.t` | uses '--validate', which wax-mb does not implement |
| `threads.t` | reads 'wasm' input; only wax is implemented |
| `to-bits-committed-int.t` | converts to/from 'wat'; only wax is implemented |
| `too-many-locals.t` | converts to/from 'wasm'; only wax is implemented |
| `trunc-operand-width.t` | converts to/from 'wasm'; only wax is implemented |
| `trytable-catch-label-self-branch.t` | converts to/from 'wat'; only wax is implemented |
| `typing-crash-recovery.t` | `cycle.wax` parses but is rejected later, so the test needs the type checker |
| `typing-diagnostics-quality.t` | converts to/from 'wat'; only wax is implemented |
| `typing-error-recovery.t` | `construction.wax` parses but is rejected later, so the test needs the type checker |
| `typing-trailing-operand-init.t` | converts to/from 'wat'; only wax is implemented |
| `typing-wrong-output.t` | converts to/from 'wat'; only wax is implemented |
| `unary-intrinsic-polymorphic-receiver.t` | converts to/from 'wat'; only wax is implemented |
| `unary-methods.t` | uses '--validate', which wax-mb does not implement |
| `unbound-local-type-no-cascade.t` | converts to/from 'wat'; only wax is implemented |
| `unbound-suggestions.t` | `func.wax` parses but is rejected later, so the test needs the type checker |
| `unfold.t` | uses '--unfold', which wax-mb does not implement |
| `unicode-escape-overflow.t` | converts to/from 'wat'; only wax is implemented |
| `unicode-identifier.t` | converts to/from 'wat'; only wax is implemented |
| `unknownref-eq-select.t` | converts to/from 'wat'; only wax is implemented |
| `unnecessary-mut.t` | exercises a lint, which needs the type checker |
| `unreachable-min-operand-cast.t` | converts to/from 'wat'; only wax is implemented |
| `unresolved-binary-ref.t` | converts to/from 'wat'; only wax is implemented |
| `unresolved-reference.t` | converts to/from 'wat'; only wax is implemented |
| `unsanitizable-label-collision.t` | converts to/from 'wat'; only wax is implemented |
| `unused-fields-kinds.t` | exercises a lint, which needs the type checker |
| `unused-fields-reachability.t` | converts to/from 'wat'; only wax is implemented |
| `unused-fields-wat.t` | exercises a lint, which needs the type checker |
| `unused-fields-wax.t` | exercises a lint, which needs the type checker |
| `unused-label-shadowed.t` | exercises a lint, which needs the type checker |
| `unused-local-shadow.t` | exercises a lint, which needs the type checker |
| `unused-locals.t` | converts to/from 'wat'; only wax is implemented |
| `unused-result-memsize.t` | exercises a lint, which needs the type checker |
| `unused-result-pure.t` | exercises a lint, which needs the type checker |
| `unused-result-simd.t` | exercises a lint, which needs the type checker |
| `unused-types-lowered-names.t` | converts to/from 'wat'; only wax is implemented |
| `unused-types.t` | exercises a lint, which needs the type checker |
| `v128-lane-range.t` | converts to/from 'wasm'; only wax is implemented |
| `validate-string-eq.t` | uses '--validate', which wax-mb does not implement |
| `validation-locations.t` | uses '--validate', which wax-mb does not implement |
| `validator-const-fold.t` | exercises a lint, which needs the type checker |
| `warn-env.t` | sets WAX_WARN, so it exercises a lint, which needs the type checker |
| `warning-levels.t` | converts to/from 'wat'; only wax is implemented |
| `wasm-parse-errors.t` | converts to/from 'wasm'; only wax is implemented |
| `wasm-select-type-mismatch.t` | reads 'wat' input; only wax is implemented |
| `wat-annotations.t` | converts to/from 'wat'; only wax is implemented |
| `wat-binary-string.t` | converts to/from 'wat'; only wax is implemented |
| `wat-memarg-range.t` | converts to/from 'wat'; only wax is implemented |
| `wat-to-wax-comments.t` | converts to/from 'wat'; only wax is implemented |
| `wat-to-wax-conditional.t` | reads 'wat' input; only wax is implemented |
| `wat-unicode-escape.t` | converts to/from 'wat'; only wax is implemented |
| `wax-annotations.t` | `unknown.wax` parses but is rejected later, so the test needs the type checker |
| `wax-block-params-expr.t` | `block.wax` parses but is rejected later, so the test needs the type checker |
| `wax-comments.t` | `comments.wax` parses but is rejected later, so the test needs the type checker |
| `wax-conditional.t` | uses '--validate', which wax-mb does not implement |
| `wax-const-expr-globals.t` | `table-local-global.wax` parses but is rejected later, so the test needs the type checker |
| `wax-duplicate-params.t` | `func.wax` parses but is rejected later, so the test needs the type checker |
| `wax-fn-type-ref.t` | `bad.wax` parses but is rejected later, so the test needs the type checker |
| `wax-forward-supertype.t` | `forward.wax` parses but is rejected later, so the test needs the type checker |
| `wax-limit-overflow.t` | uses '--all-errors', which wax-mb does not implement |
| `wax-memarg-limits.t` | `align.wax` parses but is rejected later, so the test needs the type checker |
| `wax-method-on-hole.t` | `field.wax` parses but is rejected later, so the test needs the type checker |
| `wax-operand-type-checks.t` | `float-method-bad.wax` parses but is rejected later, so the test needs the type checker |
| `wax-stack-switching-checks.t` | uses '--validate', which wax-mb does not implement |
| `wax-start.t` | converts to/from 'wat'; only wax is implemented |
| `wax-table-inline-elem.t` | converts to/from 'wat'; only wax is implemented |
| `wax-to-wat-comments.t` | converts to/from 'wat'; only wax is implemented |
| `wax-uninitialized-local.t` | `read-uninit.wax` parses but is rejected later, so the test needs the type checker |
| `while-step.t` | converts to/from 'wat'; only wax is implemented |
| `wide-arithmetic.t` | reads 'wat' input; only wax is implemented |
| `width-check-erasers.t` | converts to/from 'wasm'; only wax is implemented |
