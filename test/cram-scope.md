# Cram tests: what this port runs

Generated by `waxdiff.py classify-cram`. A test is in scope when every
`wax` command it runs uses only flags and formats wax-mb implements;
anything else is listed below with the reason, so the excluded set stays
visible instead of shrinking unnoticed.

In scope: **72** of 331.

That is a small fraction, and the reasons below say why: the
reference's cram suite is overwhelmingly about the two things this
port does not implement -- conversion to and from wat/wasm, and the
type checker with its lints. The differential oracles are what cover
the front end, over 2112 corpus files; these tests add the CLI's own
behaviour (exit codes, which stream output goes to, flag handling).

## Excluded

| test | why |
|---|---|
| `all-errors-semantic-raise.t` | reads 'wat' input; only wax input is implemented |
| `arg-type-mismatch.t` | uses '--validate', which wax-mb does not implement |
| `array-len-bottom-ref.t` | uses '--validate', which wax-mb does not implement |
| `array-new-data-datacount.t` | reads 'wasm' input; only wax input is implemented |
| `array-new-fixed-large-count.t` | reads 'wat' input; only wax input is implemented |
| `array-op-receiver-cast.t` | reads 'wat' input; only wax input is implemented |
| `bare-export.t` | reads 'wat' input; only wax input is implemented |
| `binary-bad-type-index.t` | reads 'wasm' input; only wax input is implemented |
| `binary-unbound-index.t` | reads 'wasm' input; only wax input is implemented |
| `binop-flexible-number-operand.t` | reads 'wat' input; only wax input is implemented |
| `block-binding-annotation.t` | reads 'wat' input; only wax input is implemented |
| `block-dead-code.t` | reads 'wat' input; only wax input is implemented |
| `block-kinds-inference.t` | reads 'wat' input; only wax input is implemented |
| `block-missing-param-location.t` | reads 'wat' input; only wax input is implemented |
| `block-result-inference.t` | reads 'wat' input; only wax input is implemented |
| `block-struct-trailing.t` | reads 'wat' input; only wax input is implemented |
| `bottom-ref-validation.t` | uses '--validate', which wax-mb does not implement |
| `br-if-passthrough-cast-block.t` | reads 'wat' input; only wax input is implemented |
| `br-if-passthrough-result.t` | reads 'wat' input; only wax input is implemented |
| `br-on-cast-chained-null-residual.t` | reads 'wasm' input; only wax input is implemented |
| `br-on-cast-dead-code-source.t` | reads 'wat' input; only wax input is implemented |
| `br-on-cast-fail-bottom-lub.t` | reads 'wat' input; only wax input is implemented |
| `br-on-cast-fail-null-source.t` | reads 'wasm' input; only wax input is implemented |
| `br-on-cast-multivalue.t` | uses '--validate', which wax-mb does not implement |
| `br-on-non-null-empty-label.t` | reads 'wat' input; only wax input is implemented |
| `br-on-null-bare-cast.t` | reads 'wat' input; only wax input is implemented |
| `br-on-null-bare-null.t` | reads 'wat' input; only wax input is implemented |
| `br-on-null-inferring-target.t` | reads 'wat' input; only wax input is implemented |
| `br-table-bottom-ref-targets.t` | uses '--validate', which wax-mb does not implement |
| `branch-hint-folded-roundtrip.t` | reads 'wasm' input; only wax input is implemented |
| `branch-hint-invalid.t` | reads 'wat' input; only wax input is implemented |
| `branch-hint-nested-cond.t` | reads 'wasm' input; only wax input is implemented |
| `branch-hinting.t` | reads 'wasm' input; only wax input is implemented |
| `bulk-segment-operands.t` | reads 'wasm' input; only wax input is implemented |
| `cast-lattice-consistency.t` | reads 'wasm' input; only wax input is implemented |
| `char-multibyte.t` | reads 'wasm' input; only wax input is implemented |
| `check-all-errors.t` | reads 'wat' input; only wax input is implemented |
| `check-define.t` | uses '-D', which wax-mb does not implement |
| `check.t` | reads 'txt' input; only wax input is implemented |
| `compact-import-groups.t` | reads 'wasm' input; only wax input is implemented |
| `compact-import-item-id.t` | reads 'wasm' input; only wax input is implemented |
| `compact-import-lowering.t` | reads 'wasm' input; only wax input is implemented |
| `compact-import-section.t` | reads 'wasm' input; only wax input is implemented |
| `compilation-hints-priority.t` | reads 'wasm' input; only wax input is implemented |
| `compilation-hints-wat.t` | reads 'wasm' input; only wax input is implemented |
| `compilation-hints-wax.t` | reads 'wasm' input; only wax input is implemented |
| `compound-assignment.t` | reads 'wat' input; only wax input is implemented |
| `cond-annot.t` | reads 'wat' input; only wax input is implemented |
| `cond-define.t` | reads 'wat' input; only wax input is implemented |
| `cond-export-name-split.t` | reads 'wat' input; only wax input is implemented |
| `cond-if-only-references.t` | reads 'wat' input; only wax input is implemented |
| `cond-validation.t` | uses '--validate', which wax-mb does not implement |
| `confusable-unicode.t` | reads 'wat' input; only wax input is implemented |
| `const-expr-lint.t` | reads 'wat' input; only wax input is implemented |
| `cont-new-const.t` | reads 'wat' input; only wax input is implemented |
| `cont-new-exact.t` | reads 'wat' input; only wax input is implemented |
| `convert-adaptive-operand.t` | reads 'wat' input; only wax input is implemented |
| `convert-brnull-block.t` | reads 'wat' input; only wax input is implemented |
| `custom-page-sizes.t` | reads 'wat' input; only wax input is implemented |
| `data-numeric-values.t` | reads 'wasm' input; only wax input is implemented |
| `dead-code-atomic-rmw-i32-widen.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-atomic-rmw-i64-addr.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-bottom-ref-call.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-bottom-ref-struct-cast.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-br-if-passthrough-i64.t` | reads 'wasm' input; only wax input is implemented |
| `dead-code-chained-cast-residual.t` | reads 'wasm' input; only wax input is implemented |
| `dead-code-flexible-result.t` | reads 'wasm' input; only wax input is implemented |
| `dead-code-isnull-reconnect.t` | reads 'wasm' input; only wax input is implemented |
| `dead-code-min-hole.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-ne-largeint.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-neg-select.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-passthrough-cast.t` | uses '--validate', which wax-mb does not implement |
| `dead-code-ref-ops.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-select-i31-get.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-select-width.t` | reads 'wat' input; only wax input is implemented |
| `dead-code-simd-hole-pin.t` | uses '--validate', which wax-mb does not implement |
| `dead-code-width.t` | reads 'wat' input; only wax input is implemented |
| `debug.t` | reads 'wat' input; only wax input is implemented |
| `declare-ref-func.t` | reads 'wasm' input; only wax input is implemented |
| `demote-float-method.t` | reads 'wasm' input; only wax input is implemented |
| `descriptor-types.t` | uses '--validate', which wax-mb does not implement |
| `desugar-elem-declare.t` | reads 'wat' input; only wax input is implemented |
| `desugar.t` | uses '--desugar', which wax-mb does not implement |
| `diagnostic-anchors.t` | reads 'wasm' input; only wax input is implemented |
| `dispatch.t` | reads 'wat' input; only wax input is implemented |
| `docs-correspondence.t` | reads paths outside its own directory |
| `docs-examples.t` | reads paths outside its own directory |
| `docs-intro.t` | reads paths outside its own directory |
| `docs-language.t` | reads paths outside its own directory |
| `docs-wat.t` | reads paths outside its own directory |
| `drop-void.t` | uses '--validate', which wax-mb does not implement |
| `drop-width-drift.t` | reads 'wat' input; only wax input is implemented |
| `eager-select.t` | reads 'wat' input; only wax input is implemented |
| `elem-declare-init.t` | reads 'wat' input; only wax input is implemented |
| `elem-kind-nonzero.t` | reads 'wasm' input; only wax input is implemented |
| `empty-name-section.t` | reads 'wasm' input; only wax input is implemented |
| `empty-stack-location.t` | uses '--validate', which wax-mb does not implement |
| `empty-struct.t` | reads 'wasm' input; only wax input is implemented |
| `eqref-extern-convert.t` | reads 'wat' input; only wax input is implemented |
| `exact-allocation-gating.t` | reads 'wat' input; only wax input is implemented |
| `exact-allocation.t` | uses '--validate', which wax-mb does not implement |
| `exact-func-import.t` | uses '--validate', which wax-mb does not implement |
| `exact-references.t` | uses '--validate', which wax-mb does not implement |
| `exnref-shorthand.t` | reads 'wasm' input; only wax input is implemented |
| `extern-convert-any-nonnull.t` | reads 'wat' input; only wax input is implemented |
| `extern-convert-i31-get.t` | reads 'wat' input; only wax input is implemented |
| `extern-convert-null.t` | reads 'wat' input; only wax input is implemented |
| `f32-signaling-nan.t` | reads 'wasm' input; only wax input is implemented |
| `f64-const-precision.t` | reads 'wasm' input; only wax input is implemented |
| `failed-definition-recovery.t` | reads 'wat' input; only wax input is implemented |
| `faithful.t` | reads 'wat' input; only wax input is implemented |
| `feature-attribute.t` | drives wax from a python3 script, so its invocations cannot be inspected |
| `field-call-not-array-op.t` | uses '--validate', which wax-mb does not implement |
| `float-trunc-literal.t` | reads 'wasm' input; only wax input is implemented |
| `fold-block-params.t` | uses '--fold', which wax-mb does not implement |
| `fold-errors.t` | uses '--fold', which wax-mb does not implement |
| `format.t` | reads '-f' input; only wax input is implemented |
| `from-wasm-struct-new-non-struct.t` | reads 'wat' input; only wax input is implemented |
| `func-inline-type-arity-mismatch.t` | reads 'wat' input; only wax input is implemented |
| `func-inline-type-exact-ref-mismatch.t` | uses '-X', which wax-mb does not implement |
| `global-supertype-annotation.t` | reads 'wat' input; only wax input is implemented |
| `guarded-export.t` | reads 'wat' input; only wax input is implemented |
| `hinted-conditional.t` | uses '--validate', which wax-mb does not implement |
| `hinted-ref-func.t` | uses '-s', which wax-mb does not implement |
| `hinted-wasm-to-wax.t` | reads 'wat' input; only wax input is implemented |
| `hole-array-method.t` | reads 'wat' input; only wax input is implemented |
| `hole-order-cast-of-hole.t` | reads 'wat' input; only wax input is implemented |
| `hole-simd-lane-immediate.t` | reads 'wat' input; only wax input is implemented |
| `hole-static-receiver.t` | reads 'wat' input; only wax input is implemented |
| `huge-int-literal.t` | uses '--validate', which wax-mb does not implement |
| `if-annotation-name-reservation.t` | reads 'wat' input; only wax input is implemented |
| `if-binding-annotation-load-bearing.t` | reads 'wat' input; only wax input is implemented |
| `if-binding-reinfer.t` | reads 'wat' input; only wax input is implemented |
| `if-infer-construction.t` | reads 'wat' input; only wax input is implemented |
| `if-infer-dead-fallthrough.t` | reads 'wat' input; only wax input is implemented |
| `if-infer-largeint-join.t` | reads 'wat' input; only wax input is implemented |
| `if-result-inference.t` | reads 'wat' input; only wax input is implemented |
| `implicit-type-index.t` | reads 'wat' input; only wax input is implemented |
| `implicit-type-numid-dedup.t` | reads 'wat' input; only wax input is implemented |
| `implicit-type-ref.t` | reads 'wasm' input; only wax input is implemented |
| `import-name-hint.t` | reads 'wat' input; only wax input is implemented |
| `import-order.t` | uses '--validate', which wax-mb does not implement |
| `import-reorder-conditional.t` | uses '--validate', which wax-mb does not implement |
| `inferred-block-float-literal.t` | reads 'wat' input; only wax input is implemented |
| `instr-conditional.t` | reads 'wat' input; only wax input is implemented |
| `int-to-float-cast.t` | reads 'wat' input; only wax input is implemented |
| `isnull-on-bottom-ref.t` | reads 'wat' input; only wax input is implemented |
| `label-name-folding.t` | reads 'wasm' input; only wax input is implemented |
| `label-preserve.t` | reads 'wat' input; only wax input is implemented |
| `labelled-arguments.t` | reads 'wat' input; only wax input is implemented |
| `largeint-abstract-compare.t` | reads 'wat' input; only wax input is implemented |
| `largeint-cast-to-i31.t` | reads 'wat' input; only wax input is implemented |
| `largeint-float-ops.t` | uses '--validate', which wax-mb does not implement |
| `largeint-float.t` | uses '--validate', which wax-mb does not implement |
| `largeint-int-join-i64.t` | uses '--validate', which wax-mb does not implement |
| `largeint-narrowing-store.t` | reads 'wat' input; only wax input is implemented |
| `largeint-unary-ops.t` | uses '--validate', which wax-mb does not implement |
| `largeint-wrap-to-i32.t` | reads 'wat' input; only wax input is implemented |
| `leftover-width-drift.t` | reads 'wasm' input; only wax input is implemented |
| `let-init.t` | uses '--validate', which wax-mb does not implement |
| `let-multi-recover.t` | reads 'wat' input; only wax input is implemented |
| `let-multi.t` | uses '--validate', which wax-mb does not implement |
| `lint-set-wat.t` | reads 'wat' input; only wax input is implemented |
| `local-name-priority.t` | reads 'wat' input; only wax input is implemented |
| `local-supertype-annotation.t` | reads 'wat' input; only wax input is implemented |
| `loops.t` | reads 'wat' input; only wax input is implemented |
| `lsp.t` | drives wax from a python3 script, so its invocations cannot be inspected |
| `match.t` | reads 'wat' input; only wax input is implemented |
| `memarg-range.t` | uses '--validate', which wax-mb does not implement |
| `memory-copy-addrtype.t` | reads 'wat' input; only wax input is implemented |
| `memory-name-shadowing.t` | reads 'wat' input; only wax input is implemented |
| `module-name.t` | reads 'wasm' input; only wax input is implemented |
| `multi-memory-memarg.t` | reads 'wasm' input; only wax input is implemented |
| `nan-payload.t` | reads 'wasm' input; only wax input is implemented |
| `narrow-store-i64-width.t` | reads 'wat' input; only wax input is implemented |
| `neg-literal-overflow.t` | uses '--validate', which wax-mb does not implement |
| `nested-call-lowering-linear.t` | drives wax from a python3 script, so its invocations cannot be inspected |
| `new-lints-wat.t` | reads 'wat' input; only wax input is implemented |
| `noextern-any-convert.t` | reads 'wat' input; only wax input is implemented |
| `null-hierarchy-nonany.t` | reads 'wat' input; only wax input is implemented |
| `number-trunc-to-i32.t` | reads 'wat' input; only wax input is implemented |
| `parse-error-messages.t` | reads 'wat' input; only wax input is implemented |
| `redundant-neg-zero.t` | reads 'wat' input; only wax input is implemented |
| `redundant-op-impure-operand.t` | reads 'wat' input; only wax input is implemented |
| `ref-func-undeclared-same-type.t` | reads 'wasm' input; only wax input is implemented |
| `ref-get-desc-exactness.t` | uses '-X', which wax-mb does not implement |
| `ref-to-int-cast-always-fails.t` | reads 'wat' input; only wax input is implemented |
| `reinterpret-literal.t` | reads 'wasm' input; only wax input is implemented |
| `resume-handler-inference.t` | reads 'wat' input; only wax input is implemented |
| `roundtrip-cast-desc-eq.t` | reads 'wat' input; only wax input is implemented |
| `segment-name-handling.t` | reads 'wat' input; only wax input is implemented |
| `select-branch-inference.t` | reads 'wat' input; only wax input is implemented |
| `select-i64-width-pin.t` | reads 'wat' input; only wax input is implemented |
| `select-largeint-branch.t` | reads 'wat' input; only wax input is implemented |
| `setter-not-a-value.t` | reads 'wat' input; only wax input is implemented |
| `simd-name-clash.t` | reads 'wat' input; only wax input is implemented |
| `simd-roundtrip.t` | reads 'wat' input; only wax input is implemented |
| `simd-trunc-sat-zero.t` | reads 'wat' input; only wax input is implemented |
| `sink-let-into-init.t` | reads 'wat' input; only wax input is implemented |
| `source-map-mappings.t` | drives wax from a python3 script, so its invocations cannot be inspected |
| `source-map.t` | uses '--source-map', which wax-mb does not implement |
| `stack-switching-errors.t` | uses '--validate', which wax-mb does not implement |
| `stack-switching.t` | reads 'wasm' input; only wax input is implemented |
| `string-char-literals.t` | reads 'wasm' input; only wax input is implemented |
| `string-global-export-index.t` | reads 'wat' input; only wax input is implemented |
| `string-i16.t` | reads 'wasm' input; only wax input is implemented |
| `struct-default-abstract-reftype.t` | reads 'wat' input; only wax input is implemented |
| `struct-punning.t` | reads 'wat' input; only wax input is implemented |
| `struct-splice-decompile.t` | reads 'wat' input; only wax input is implemented |
| `struct-splice.t` | runs the '-' subcommand, which wax-mb does not implement |
| `structured-try.t` | reads 'wasm' input; only wax input is implemented |
| `switch-tag-continuation.t` | reads 'wat' input; only wax input is implemented |
| `syntactic-checks.t` | uses '--validate', which wax-mb does not implement |
| `syntax-error-fix.t` | reads 'wat' input; only wax input is implemented |
| `tag-no-payload.t` | uses '--validate', which wax-mb does not implement |
| `text-type-errors.t` | uses '--validate', which wax-mb does not implement |
| `threads.t` | reads 'wasm' input; only wax input is implemented |
| `too-many-locals.t` | reads 'wasm' input; only wax input is implemented |
| `trunc-operand-width.t` | reads 'wasm' input; only wax input is implemented |
| `trytable-catch-label-self-branch.t` | reads 'wat' input; only wax input is implemented |
| `typing-diagnostics-quality.t` | reads 'wat' input; only wax input is implemented |
| `typing-trailing-operand-init.t` | uses '-X', which wax-mb does not implement |
| `typing-wrong-output.t` | uses '-D', which wax-mb does not implement |
| `unary-intrinsic-polymorphic-receiver.t` | reads 'wat' input; only wax input is implemented |
| `unary-methods.t` | uses '--validate', which wax-mb does not implement |
| `unbound-local-type-no-cascade.t` | reads 'wat' input; only wax input is implemented |
| `unfold.t` | uses '--unfold', which wax-mb does not implement |
| `unknownref-eq-select.t` | reads 'wat' input; only wax input is implemented |
| `unnecessary-mut.t` | reads 'wat' input; only wax input is implemented |
| `unreachable-min-operand-cast.t` | reads 'wat' input; only wax input is implemented |
| `unresolved-binary-ref.t` | reads 'wat' input; only wax input is implemented |
| `unresolved-reference.t` | reads 'wat' input; only wax input is implemented |
| `unsanitizable-label-collision.t` | reads 'wat' input; only wax input is implemented |
| `unused-fields-kinds.t` | reads 'wat' input; only wax input is implemented |
| `unused-fields-reachability.t` | reads 'wat' input; only wax input is implemented |
| `unused-fields-wat.t` | reads 'wat' input; only wax input is implemented |
| `unused-locals.t` | uses '--validate', which wax-mb does not implement |
| `unused-types-lowered-names.t` | reads 'wat' input; only wax input is implemented |
| `unused-types.t` | reads 'wat' input; only wax input is implemented |
| `v128-lane-range.t` | uses '--validate', which wax-mb does not implement |
| `validate-string-eq.t` | uses '--validate', which wax-mb does not implement |
| `validation-locations.t` | uses '--validate', which wax-mb does not implement |
| `validator-const-fold.t` | reads 'wat' input; only wax input is implemented |
| `warning-levels.t` | reads 'wat' input; only wax input is implemented |
| `wasm-parse-errors.t` | reads 'wasm' input; only wax input is implemented |
| `wasm-select-type-mismatch.t` | reads 'wat' input; only wax input is implemented |
| `wat-annotations.t` | reads 'wat' input; only wax input is implemented |
| `wat-binary-string.t` | reads 'wat' input; only wax input is implemented |
| `wat-memarg-range.t` | reads 'wat' input; only wax input is implemented |
| `wat-to-wax-comments.t` | reads 'wat' input; only wax input is implemented |
| `wat-to-wax-conditional.t` | reads 'wat' input; only wax input is implemented |
| `wat-unicode-escape.t` | reads 'wat' input; only wax input is implemented |
| `wax-conditional.t` | uses '--validate', which wax-mb does not implement |
| `wax-stack-switching-checks.t` | uses '--validate', which wax-mb does not implement |
| `wax-start.t` | reads 'wat' input; only wax input is implemented |
| `wax-table-inline-elem.t` | reads 'wat' input; only wax input is implemented |
| `while-step.t` | reads 'wat' input; only wax input is implemented |
| `wide-arithmetic.t` | reads 'wat' input; only wax input is implemented |
| `width-check-erasers.t` | reads 'wasm' input; only wax input is implemented |
