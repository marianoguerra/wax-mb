{
  "_comment": [
    "The pinned reference implementation, used as the oracle by the differential",
    "harness (cmd/waxdiff). Restore it with tools/fetch-reference.sh.",
    "",
    "NOTE the release is 'edge', which ocsigen/wax REBUILDS ON EVERY PUSH to main.",
    "The asset URL therefore serves a moving target, which is why sha256 and",
    "upstream_commit are recorded here: fetch-reference.sh fails loudly when what",
    "it downloads is not what this file describes, rather than silently swapping",
    "the oracle out from under the goldens.",
    "",
    "'edge' is also the ONLY place upstream publishes a build: there is no",
    "per-commit asset, and the one tagged release (v0.1.0) is 328 commits behind.",
    "So the moment 'edge' is rebuilt, the binary these goldens were made with",
    "stops being obtainable from upstream at all -- which is what `mirror` is for.",
    "It names a release of OUR OWN holding a copy of exactly the pinned build,",
    "and fetch-reference.sh reaches for it FIRST. sha256 stays the authority",
    "either way: a mirror that does not match the pin is refused like any other.",
    "",
    "The npm package @wax-wasm/wax is NOT usable as the oracle: it is pinned at",
    "0.1.0, which is 328 commits behind main, and the front end diverged heavily",
    "over those commits (parser.mly 558 lines, output.ml 377, lexer.ml 91, plus",
    "the whole parser_messages generator, which did not exist at 0.1.0)."
  ],
  "repo": "ocsigen/wax",
  "release": "edge",
  "mirror_repo": "marianoguerra/wax-mb",
  "mirror_release": "reference-209f43a",
  "asset": "wax-linux-x86_64",
  "sha256": "abbee1be4e19b1b76c61269bc6966a35b088ff3d257b7c15bb59dfc904252ac6",
  "reports_version": "0.1.1~dev",
  "upstream_commit": "209f43a",
  "ported_from_commit": "209f43a9526404c73a6b433f35a24c6b54f2a5f9",
  "front_end_identical": true,
  "_front_end_note": [
    "upstream_commit and ported_from_commit are the SAME commit: the binary is",
    "built from exactly the sources in wax/, so there is no skew to reason about.",
    "Re-check after any bump with:",
    "  gh api repos/ocsigen/wax/compare/<ported_from_commit>...<upstream_commit>"
  ]
}
